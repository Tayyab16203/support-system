{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint on Save",
      "trigger": "PostFileSave",
      "description": "Runs ESLint for TypeScript/TSX files and Ruff for Python files when saved",
      "matcher": "\\.(ts|tsx)$",
      "action": {
        "type": "command",
        "command": "npx eslint --fix --no-warn-ignored",
        "timeout": 30
      }
    }
  ]
}